import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  errorWrapper: {
    position: "absolute",
    right: "10px",
    bottom: "100px",
    zIndex: "9999",
  },
});
