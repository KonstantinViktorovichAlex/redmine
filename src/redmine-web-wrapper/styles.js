import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    minHeight: "100vh",
  },
  errorWrapper: {
    position: "absolute",
    top: "10px",
    right: "10px",
  },
});
